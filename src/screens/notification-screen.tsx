import { ScrollView, Text } from "react-native";

export default function NotificationScreen() {
    return (
        <ScrollView>
            {/* Seção: Cards Destacados */}
            <Text>Notificacoes Screen</Text>
        </ScrollView>
    );
}
